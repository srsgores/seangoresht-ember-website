{{page-title this.model.name}}

<div class="gallery-active-photo" aria-live="assertive" aria-label="Current Photo">
	<Gallery::Photo @photo={{this.model}}>
		<LinkTo @route="photography" class="close" title="Back to all pictures">❌</LinkTo>
	</Gallery::Photo>

	{{outlet}}
</div>