<div class="work-history" ...attributes>
	{{#if this.sortedRoles.length}}
		<ol class="work-history-timeline">
			{{#each this.sortedRoles as |role|}}
				<li class="role-container">
					<Resume::Role @role={{role}}/>
				</li>
			{{/each}}
		</ol>
	{{/if}}
	{{yield}}
</div>
